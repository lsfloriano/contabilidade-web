# Estorno de lançamentos — plano de implementação

Fonte: `docs/superpowers/specs/2026-09-21-estorno-design.md` (aprovado).

Branch: `feat/estorno`, **já criada e já em uso** a partir de `master`. Não
crie, não troque de branch, não rebase. Cada tarefa termina com um commit
nessa branch.

## O que este trabalho entrega

A mecânica contábil de **estorno**: em vez de editar ou apagar um lançamento
errado, cria-se um lançamento reverso (débito e crédito trocados, mesmo valor)
que referencia o original.

- Backend: uma coluna nova (`estorno_de`), um schema novo
  (`LancamentoEstornoCreate`), um campo novo em `LancamentoOut`, uma rota nova
  (`POST /lancamentos/{lancamento_id}/estorno`) e um helper de histórico
  padrão.
- Frontend: um botão "Estornar" em cada linha da tabela de lançamentos, um
  "modo estorno" no formulário que já existe, e duas indicações visuais na
  tabela derivadas de `estorno_de`.

**Nenhum relatório muda.** Verificado contra o código, não assumido:
`calcular_balancete` (`backend/app/relatorios.py:61-72`) soma
`groupby("conta_debito")` e `groupby("conta_credito")` separadamente e depois
faz `saldo = total_debito - total_credito` (devedora) ou o inverso (credora).
Um original (débito A, crédito B, valor V) mais o estorno (débito B, crédito A,
valor V) somam, para A, `total_debito = V` e `total_credito = V` — logo
`saldo = 0`; idem para B. **Atenção ao que isso significa para os testes:** o
que volta a zero é o **`saldo`**; as colunas `total_debito` e `total_credito`
de cada conta **crescem** (passam a V cada uma), e
`montar_balancete()["total_debito"]`/`["total_credito"]` passam de V para 2V.
O estorno cancela o original no saldo, não apaga o movimento — que é
exatamente o comportamento contábil desejado. `backend/app/relatorios.py`
**não é editado**.

## Fora de escopo (não implemente, mesmo se parecer natural)

- Editar ou excluir um lançamento existente.
- Qualquer restrição sobre quantas vezes um lançamento pode ser estornado, ou
  sobre estornar um estorno. As duas coisas são permitidas, de propósito.
- Flag `estornado` no lançamento original. `estorno_de` existe **só** no
  estorno, apontando para o original.
- Migração de banco (`ALTER TABLE` condicional). `init_db` só roda
  `create_all`.
- Endpoint novo de leitura para as indicações visuais: elas saem de
  `GET /lancamentos`, que passa a devolver `estorno_de`.

## Passo manual do coordenador (não é tarefa, não é fixture)

Depois que este trabalho estiver implementado, **antes de rodar o app
localmente**, o coordenador precisa apagar `backend/contabilidade.db` à mão
para que a tabela `lancamentos` seja recriada com a coluna `estorno_de`.
Isto **não** aparece como passo de nenhuma tarefa e **não** é assunto de
`conftest.py`: os testes usam `sqlite:///:memory:` com `init_db` por teste
(`backend/tests/conftest.py:10-19`), então já nascem com a coluna nova.

## Restrições globais (valem para todas as tarefas)

- **Zero dependências novas.** `frontend/package.json`,
  `frontend/package-lock.json` e `backend/requirements.txt` não podem aparecer
  no diff.
- **`backend/app/relatorios.py` não é editado.** Se alguma tarefa parecer
  exigir isso, **pare e reporte** — é defeito de plano, não algo para
  contornar.
- **Zero CSS novo.** `frontend/src/index.css` **não é editado**. Todas as
  classes usadas já existem e foram conferidas no arquivo:
  `.form-lancamento` (162), `.campo` (170), `.campo-rotulo` (177),
  `.botao` (212), `.erro` (255), `.tabela-rolagem` (156), `.razao-valor` (119),
  `.valor-negativo` (125, usada via `classeValor`).
- **`validar_lancamento` é chamada exatamente como em `criar_lancamento`:**
  `validar_lancamento(db, conta_debito, conta_credito, valor)` dentro de
  `try`, com `except LancamentoInvalido as exc:` virando
  `HTTPException(status_code=422, detail=str(exc))`. Não mude a assinatura de
  `validar_lancamento`, não crie validação nova.
- **Idioma:** toda string visível ao usuário em português (pt-BR).
- **Sem framework de teste de frontend.** Decisão de escopo deliberada do
  projeto. Não adicione Vitest/Jest/Testing Library. O portão automatizado da
  Tarefa 2 é `npm run build` passando a partir de `frontend/`. A verificação
  visual no navegador é feita pelo coordenador, depois.
- **Backend tem pytest de verdade.** Fixtures `db_session` e `client` de
  `backend/tests/conftest.py`, `Lancamento(...)` com `Decimal`, asserts
  diretos, comentário com a aritmética conferida à mão — o estilo dos testes
  já existentes. A suíte hoje tem **52 testes passando** (verificado:
  `python -m pytest -q` → `52 passed`); ao fim da Tarefa 1 deve ter **59**, com
  os 52 antigos intactos. A Tarefa 2 não acrescenta teste automatizado.
- **Estilo de consulta do repositório:** busca por chave é
  `db.query(Modelo).filter_by(...).first()` (como em `app/validacao.py`), não
  `db.get(...)`. Mantenha.
- **Ordem:** Tarefa 1 (backend) antes da Tarefa 2 (frontend). A Tarefa 2
  consome o contrato do endpoint.

---

## Tarefa 1 — coluna `estorno_de`, schemas, rota de estorno e testes

### Arquivos

- `backend/app/models.py` — **modificar** (uma linha nova em `Lancamento`)
- `backend/app/schemas.py` — **modificar** (um campo novo, uma classe nova)
- `backend/app/routers/lancamentos.py` — **modificar** (um import, um helper,
  uma rota — nada existente é editado além da linha de import de schemas)
- `backend/tests/test_lancamentos_api.py` — **modificar** (só acrescenta
  testes ao fim do arquivo)

### Interfaces

Esta tarefa **produz**:

- `Lancamento.estorno_de` (`backend/app/models.py`) — `Column(Integer,
  ForeignKey("lancamentos.id"), nullable=True)`. Sem `relationship`.
- `LancamentoOut.estorno_de` (`backend/app/schemas.py`) — `Optional[int] =
  None`. Como `LancamentoOut` herda de `LancamentoCreate`, esse campo aparece
  em **todas** as respostas de lançamento: `POST /lancamentos`,
  `GET /lancamentos` e a rota nova. Em lançamento normal vem `null`.
- `LancamentoEstornoCreate` (`backend/app/schemas.py`) — corpo da rota nova:
  `data: date` (obrigatório), `historico: Optional[str] = None`.
- `_historico_padrao_estorno(lancamento_id: int, historico_original: str |
  None) -> str` (`backend/app/routers/lancamentos.py`) — devolve
  `f"Estorno do lançamento #{lancamento_id}: {historico_original}"`, ou
  `f"Estorno do lançamento #{lancamento_id}"` quando o original não tem
  histórico.
- `POST /lancamentos/{lancamento_id}/estorno` — `201` com um `LancamentoOut`;
  `404` se `lancamento_id` não existir; `422` se `validar_lancamento` recusar.

Esta tarefa **consome** o que já existe: `Lancamento` (`app.models`),
`validar_lancamento` e `LancamentoInvalido` (`app.validacao`), `get_db`
(`app.db`), e as fixtures `client`/`db_session` de `backend/tests/conftest.py`
(SQLite em memória, recriado e semeado a cada teste).

### Passos

- [ ] **Acrescentar a coluna em `Lancamento`** (`backend/app/models.py`).
      Depois da linha `historico = Column(String, nullable=True)` (linha 47) e
      **antes** da linha em branco que precede `debito = relationship(...)`,
      insira exatamente:

```python
    # Só o estorno tem este campo preenchido, apontando para o lançamento que
    # ele reverte. O original não ganha flag nenhuma: "foi estornado?" se
    # deriva de existir outro lançamento com estorno_de == id dele.
    estorno_de = Column(Integer, ForeignKey("lancamentos.id"), nullable=True)
```

      `Integer` e `ForeignKey` já estão importados na linha 3; **não** mexa nos
      imports. **Não** adicione `relationship` para essa FK.

- [ ] **Acrescentar o campo em `LancamentoOut`** (`backend/app/schemas.py`).
      Substitua o bloco das linhas 25-28 por:

```python
class LancamentoOut(LancamentoCreate):
    model_config = ConfigDict(from_attributes=True)

    id: int
    estorno_de: Optional[int] = None


class LancamentoEstornoCreate(BaseModel):
    # O estorno não escolhe contas nem valor: os três saem invertidos/iguais
    # do lançamento original. Só data e histórico são do usuário.
    data: date
    historico: Optional[str] = None
```

      `Optional`, `date`, `BaseModel` e `ConfigDict` já estão importados
      (linhas 1-4). Nada mais no arquivo muda.

- [ ] **Escrever os testes novos** — acrescente ao **fim** de
      `backend/tests/test_lancamentos_api.py` (os 5 testes existentes ficam
      intactos) exatamente este conteúdo:

```python
def test_estornar_lancamento_inverte_contas_valor_e_referencia(client):
    original = client.post(
        "/lancamentos",
        json={
            "data": "2026-01-05",
            "conta_debito": "1.1.01",
            "conta_credito": "2.3.01",
            "valor": 1000.0,
            "historico": "Integralização de capital",
        },
    ).json()

    resposta = client.post(f"/lancamentos/{original['id']}/estorno", json={"data": "2026-02-01"})

    assert resposta.status_code == 201
    corpo = resposta.json()
    assert corpo["id"] != original["id"]
    assert corpo["data"] == "2026-02-01"
    assert corpo["conta_debito"] == "2.3.01"  # era o crédito do original
    assert corpo["conta_credito"] == "1.1.01"  # era o débito do original
    assert corpo["valor"] == 1000.0
    assert corpo["estorno_de"] == original["id"]
    assert corpo["historico"] == f"Estorno do lançamento #{original['id']}: Integralização de capital"
    # O original não muda: estorno_de vive só no estorno.
    assert original["estorno_de"] is None


def test_estornar_lancamento_historico_padrao_sem_historico_original(client):
    original = client.post(
        "/lancamentos",
        json={"data": "2026-01-05", "conta_debito": "1.1.01", "conta_credito": "2.3.01", "valor": 50.0},
    ).json()

    resposta = client.post(f"/lancamentos/{original['id']}/estorno", json={"data": "2026-02-01"})

    assert resposta.status_code == 201
    # Sem ": " pendurado no fim quando o original não tinha histórico.
    assert resposta.json()["historico"] == f"Estorno do lançamento #{original['id']}"


def test_estornar_lancamento_respeita_historico_customizado(client):
    original = client.post(
        "/lancamentos",
        json={
            "data": "2026-01-05",
            "conta_debito": "1.1.01",
            "conta_credito": "2.3.01",
            "valor": 50.0,
            "historico": "Integralização de capital",
        },
    ).json()

    resposta = client.post(
        f"/lancamentos/{original['id']}/estorno",
        json={"data": "2026-02-01", "historico": "Estorno por erro de digitação"},
    )

    assert resposta.status_code == 201
    assert resposta.json()["historico"] == "Estorno por erro de digitação"


def test_estornar_lancamento_inexistente_retorna_404(client):
    resposta = client.post("/lancamentos/999/estorno", json={"data": "2026-02-01"})
    assert resposta.status_code == 404


def test_estornar_um_estorno_e_permitido(client):
    # O app não valida lançamento duplicado em lugar nenhum; estornar um
    # estorno (o que recria o lançamento original) segue a mesma regra.
    original = client.post(
        "/lancamentos",
        json={
            "data": "2026-01-05",
            "conta_debito": "1.1.01",
            "conta_credito": "2.3.01",
            "valor": 1000.0,
            "historico": "Integralização de capital",
        },
    ).json()
    estorno = client.post(f"/lancamentos/{original['id']}/estorno", json={"data": "2026-02-01"}).json()

    resposta = client.post(f"/lancamentos/{estorno['id']}/estorno", json={"data": "2026-03-01"})

    assert resposta.status_code == 201
    corpo = resposta.json()
    # Inverter duas vezes volta às contas do original.
    assert corpo["conta_debito"] == "1.1.01"
    assert corpo["conta_credito"] == "2.3.01"
    assert corpo["estorno_de"] == estorno["id"]
    assert corpo["historico"] == (
        f"Estorno do lançamento #{estorno['id']}: "
        f"Estorno do lançamento #{original['id']}: Integralização de capital"
    )


def test_listar_lancamentos_devolve_estorno_de(client):
    original = client.post(
        "/lancamentos",
        json={"data": "2026-01-05", "conta_debito": "1.1.01", "conta_credito": "2.3.01", "valor": 1000.0},
    ).json()
    estorno = client.post(f"/lancamentos/{original['id']}/estorno", json={"data": "2026-02-01"}).json()

    corpo = client.get("/lancamentos").json()

    # Ordenado por data (ordenação já existente da rota): original, depois estorno.
    assert [l["id"] for l in corpo] == [original["id"], estorno["id"]]
    assert corpo[0]["estorno_de"] is None
    assert corpo[1]["estorno_de"] == original["id"]


def test_estorno_zera_o_saldo_das_contas_no_balancete(client, db_session):
    # Teste de fechamento: a prova de que o estorno cancela o original pela
    # própria aritmética da partida dobrada, sem calcular_balancete saber que
    # "estorno" existe.
    #
    # Antes:  1.1.01 débito 1000 / 2.3.01 crédito 1000
    #   Caixa (devedora):  1000 - 0 = 1000
    #   Capital (credora): 1000 - 0 = 1000
    # Depois do estorno (2.3.01 débito 1000 / 1.1.01 crédito 1000):
    #   Caixa:   débitos 1000, créditos 1000 -> saldo 1000 - 1000 = 0
    #   Capital: créditos 1000, débitos 1000 -> saldo 1000 - 1000 = 0
    # Os totais de débito e crédito NÃO zeram: cada conta passa a ter 1000 nos
    # dois lados, e a soma geral do balancete vai de 1000 para 2000. O estorno
    # cancela o saldo, não apaga o movimento.
    original = client.post(
        "/lancamentos",
        json={"data": "2026-01-05", "conta_debito": "1.1.01", "conta_credito": "2.3.01", "valor": 1000.0},
    ).json()

    antes = montar_balancete(db_session)
    caixa_antes = next(c for c in antes["linhas"] if c["codigo"] == "1.1.01")
    capital_antes = next(c for c in antes["linhas"] if c["codigo"] == "2.3.01")
    assert caixa_antes["saldo"] == 1000.00
    assert capital_antes["saldo"] == 1000.00
    assert antes["total_debito"] == 1000.00
    assert antes["total_credito"] == 1000.00

    assert client.post(f"/lancamentos/{original['id']}/estorno", json={"data": "2026-02-01"}).status_code == 201

    depois = montar_balancete(db_session)
    caixa_depois = next(c for c in depois["linhas"] if c["codigo"] == "1.1.01")
    capital_depois = next(c for c in depois["linhas"] if c["codigo"] == "2.3.01")
    assert caixa_depois["saldo"] == 0.0
    assert capital_depois["saldo"] == 0.0
    assert caixa_depois["total_debito"] == 1000.00
    assert caixa_depois["total_credito"] == 1000.00
    assert depois["total_debito"] == 2000.00
    assert depois["total_credito"] == 2000.00
```

- [ ] **Acrescentar o import que o último teste usa** — no **topo** de
      `backend/tests/test_lancamentos_api.py`, que hoje começa direto em
      `def test_criar_lancamento_valido(client):`, insira antes de tudo:

```python
from app.relatorios import montar_balancete


```

      (uma linha de import e duas linhas em branco antes do primeiro `def`).

- [ ] **Rodar os testes e confirmar que os novos falham** — a partir de
      `backend/`:

```
python -m pytest -q
```

      Esperado: `6 failed, 53 passed`. Seis dos sete testes novos quebram
      porque `/lancamentos/{id}/estorno` ainda não casa com nenhuma rota
      registrada e o FastAPI devolve `404 Not Found` (não `405`: o caminho não
      existe, não é o método que está errado) — uns falham no
      `assert ... == 201`, outros em `KeyError: 'id'` ao ler o corpo
      `{"detail": "Not Found"}`. O sétimo,
      `test_estornar_lancamento_inexistente_retorna_404`, **passa desde já**,
      pelo motivo errado: o 404 vem de a rota não existir, não de o lançamento
      não existir. Isso é esperado e não é motivo para reescrevê-lo — depois
      da implementação ele passa pelo motivo certo.

- [ ] **Escrever a rota** (`backend/app/routers/lancamentos.py`). Primeiro,
      troque a linha 9 (import de schemas) por:

```python
from app.schemas import (
    LancamentoCreate,
    LancamentoEstornoCreate,
    LancamentoOut,
    UploadErro,
    UploadResultado,
)
```

      Depois, insira o helper e a rota **entre** `criar_lancamento` (que
      termina em `return lancamento`) e `listar_lancamentos` — ou seja, logo
      antes do decorador `@router.get("/lancamentos", ...)` —, separados por
      duas linhas em branco de cada lado:

```python
def _historico_padrao_estorno(lancamento_id: int, historico_original: str | None) -> str:
    if historico_original:
        return f"Estorno do lançamento #{lancamento_id}: {historico_original}"
    return f"Estorno do lançamento #{lancamento_id}"


@router.post("/lancamentos/{lancamento_id}/estorno", response_model=LancamentoOut, status_code=201)
def estornar_lancamento(
    lancamento_id: int,
    payload: LancamentoEstornoCreate,
    db: Session = Depends(get_db),
):
    original = db.query(Lancamento).filter_by(id=lancamento_id).first()
    if original is None:
        raise HTTPException(status_code=404, detail=f"lançamento #{lancamento_id} não existe")

    # O estorno é o reverso exato: os dois lados trocam, o valor é o mesmo.
    conta_debito = original.conta_credito
    conta_credito = original.conta_debito
    valor = float(original.valor)

    # A mesma validação de POST /lancamentos. Redundante na prática (o reverso
    # de um lançamento válido é sempre válido), mas sem custo e consistente.
    try:
        validar_lancamento(db, conta_debito, conta_credito, valor)
    except LancamentoInvalido as exc:
        raise HTTPException(status_code=422, detail=str(exc))

    lancamento = Lancamento(
        data=payload.data,
        conta_debito=conta_debito,
        conta_credito=conta_credito,
        valor=valor,
        historico=payload.historico or _historico_padrao_estorno(lancamento_id, original.historico),
        estorno_de=lancamento_id,
    )
    db.add(lancamento)
    db.commit()
    db.refresh(lancamento)
    return lancamento
```

      Nada mais no arquivo muda: `criar_lancamento`, `listar_lancamentos` e
      `upload_lancamentos` ficam como estão, e `Depends`, `HTTPException`,
      `Session`, `get_db`, `Lancamento`, `validar_lancamento` e
      `LancamentoInvalido` já estão importados.

- [ ] **Rodar os testes e confirmar que passam** — a partir de `backend/`:

```
python -m pytest -q
```

      Esperado: `59 passed`.

- [ ] **Commitar** na branch `feat/estorno`:

```
git add backend/app/models.py backend/app/schemas.py backend/app/routers/lancamentos.py backend/tests/test_lancamentos_api.py
git commit -m "Estorno de lançamentos: coluna estorno_de, schema e rota POST /lancamentos/{id}/estorno"
```

---

## Tarefa 2 — `estornarLancamento()` e o modo estorno da página Lançamentos

### Arquivos

- `frontend/src/api.js` — **modificar** (uma função nova)
- `frontend/src/pages/Lancamentos.jsx` — **modificar** (conteúdo completo
  abaixo)

### Interfaces

Esta tarefa **consome** o backend da Tarefa 1:

- `POST /lancamentos/{id}/estorno`, corpo JSON `{ "data": "AAAA-MM-DD",
  "historico": string | null }`, resposta `201` com o lançamento criado.
- `GET /lancamentos` devolve, em cada item, os campos `id`, `data`,
  `conta_debito`, `conta_credito`, `valor` (number), `historico`
  (string | null) e **`estorno_de` (number | null)**.

Esta tarefa **produz**:

- `estornarLancamento(id, dados)` em `frontend/src/api.js`, exportada, onde
  `dados` é `{ data, historico }`.

Também consome, já existentes: `getContas`, `getLancamentos`,
`criarLancamento`, `uploadLancamentos` (`../api`) e `fmt`, `classeValor`,
`formatarData` (`../components/graficos-comuns`).

### A máquina de estados do formulário

Um único estado novo, `estornoDe` (`number | null`), governa tudo; o derivado
`emModoEstorno = estornoDe !== null` é o que a marcação consulta.

| | Modo normal (`estornoDe === null`) | Modo estorno (`estornoDe === id`) |
|---|---|---|
| `data` | livre, vazia | livre, vazia (usuário preenche) |
| `conta_debito` | livre | **`disabled`**, = crédito do original |
| `conta_credito` | livre | **`disabled`**, = débito do original |
| `valor` | livre | **`disabled`**, = valor do original |
| `historico` | livre | livre, pré-preenchido com o padrão |
| rótulo do submit | `"Lançar"` | `"Confirmar estorno"` |
| endpoint do submit | `criarLancamento` | `estornarLancamento(estornoDe, …)` |
| botão "Cancelar" | ausente | presente |

Transições:

- **Clicar "Estornar" na linha N** → entra no modo: `estornoDe = N`, campos
  preenchidos invertidos, `erroForm` limpo. Clicar "Estornar" em outra linha
  enquanto já está no modo simplesmente troca o alvo.
- **Clicar "Cancelar"** → volta ao modo normal: `estornoDe = null`, formulário
  vazio, `erroForm` limpo, **nada é enviado**.
- **Submit bem-sucedido** → volta ao modo normal e limpa o formulário, do
  mesmo jeito que lançar um lançamento comum já faz hoje.
- **Submit com erro** → **permanece** no modo estorno com os campos
  preenchidos, e `erroForm` mostra a mensagem (mesmo tratamento do modo
  normal).

### Passos

- [ ] **Acrescentar `estornarLancamento` em `frontend/src/api.js`**, logo
      depois de `criarLancamento` (que termina na linha 29) e antes de
      `uploadLancamentos`, separada por uma linha em branco de cada lado:

```javascript
// `dados` é { data, historico }: contas e valor não vão no corpo — o backend
// os deriva do lançamento original, invertendo os dois lados.
export function estornarLancamento(id, dados) {
  return fetch(`${API_BASE}/lancamentos/${id}/estorno`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(dados),
  }).then(handleResponse);
}
```

      Nada mais no arquivo muda.

- [ ] **Substituir `frontend/src/pages/Lancamentos.jsx` inteiro** por este
      conteúdo:

```jsx
import { useEffect, useState } from "react";
import {
  getContas,
  getLancamentos,
  criarLancamento,
  estornarLancamento,
  uploadLancamentos,
} from "../api";
import { fmt, classeValor, formatarData } from "../components/graficos-comuns";

const LANCAMENTO_VAZIO = {
  data: "",
  conta_debito: "",
  conta_credito: "",
  valor: "",
  historico: "",
};

// O mesmo texto que o backend geraria se `historico` fosse enviado vazio
// (_historico_padrao_estorno, em routers/lancamentos.py). Duplicado de
// propósito: o campo já chega preenchido e editável, sem uma ida ao servidor
// só para descobrir o rótulo padrão.
function historicoPadraoEstorno(lancamento) {
  return lancamento.historico
    ? `Estorno do lançamento #${lancamento.id}: ${lancamento.historico}`
    : `Estorno do lançamento #${lancamento.id}`;
}

// id do original -> id do estorno que aponta para ele. Nada impede dois
// estornos do mesmo lançamento (o app não valida duplicidade em lugar
// nenhum); nesse caso a indicação cita o de menor id — determinístico
// independente da ordem da lista, que vem ordenada por data.
function mapearEstornos(lancamentos) {
  const mapa = {};
  lancamentos.forEach((lancamento) => {
    if (lancamento.estorno_de == null) return;
    const atual = mapa[lancamento.estorno_de];
    if (atual === undefined || lancamento.id < atual) {
      mapa[lancamento.estorno_de] = lancamento.id;
    }
  });
  return mapa;
}

export default function Lancamentos() {
  const [contas, setContas] = useState([]);
  const [lancamentos, setLancamentos] = useState([]);
  const [form, setForm] = useState(LANCAMENTO_VAZIO);
  const [estornoDe, setEstornoDe] = useState(null);
  const [erroForm, setErroForm] = useState(null);
  const [resultadoUpload, setResultadoUpload] = useState(null);

  const emModoEstorno = estornoDe !== null;
  const estornadoPor = mapearEstornos(lancamentos);

  async function carregarDados() {
    const [contasResp, lancamentosResp] = await Promise.all([getContas(), getLancamentos()]);
    setContas(contasResp);
    setLancamentos(lancamentosResp);
  }

  useEffect(() => {
    carregarDados().catch((e) => setErroForm(e.message));
  }, []);

  async function aoSubmeter(evento) {
    evento.preventDefault();
    setErroForm(null);
    try {
      if (emModoEstorno) {
        // Contas e valor não vão: o backend os deriva do original.
        await estornarLancamento(estornoDe, { data: form.data, historico: form.historico });
        setEstornoDe(null);
      } else {
        await criarLancamento({ ...form, valor: parseFloat(form.valor) });
      }
      setForm(LANCAMENTO_VAZIO);
      await carregarDados();
    } catch (erro) {
      // Erro mantém o modo e os campos como estavam, para poder corrigir a
      // data e tentar de novo.
      setErroForm(erro.message);
    }
  }

  function aoEstornar(lancamento) {
    setErroForm(null);
    setEstornoDe(lancamento.id);
    setForm({
      data: "",
      conta_debito: lancamento.conta_credito,
      conta_credito: lancamento.conta_debito,
      valor: String(lancamento.valor),
      historico: historicoPadraoEstorno(lancamento),
    });
  }

  function aoCancelarEstorno() {
    setErroForm(null);
    setEstornoDe(null);
    setForm(LANCAMENTO_VAZIO);
  }

  async function aoSelecionarArquivo(evento) {
    const arquivo = evento.target.files[0];
    if (!arquivo) return;
    setErroForm(null);
    try {
      const resultado = await uploadLancamentos(arquivo);
      setResultadoUpload(resultado);
      await carregarDados();
    } catch (erro) {
      setErroForm(erro.message);
    }
    evento.target.value = "";
  }

  return (
    <section>
      <h2>Lançamentos</h2>

      <form className="form-lancamento" onSubmit={aoSubmeter}>
        <label className="campo">
          <span className="campo-rotulo">Data</span>
          <input
            type="date"
            value={form.data}
            onChange={(e) => setForm({ ...form, data: e.target.value })}
            required
          />
        </label>
        <label className="campo">
          <span className="campo-rotulo">Conta débito</span>
          <select
            value={form.conta_debito}
            onChange={(e) => setForm({ ...form, conta_debito: e.target.value })}
            disabled={emModoEstorno}
            required
          >
            <option value="">Selecione</option>
            {contas.map((conta) => (
              <option key={conta.codigo} value={conta.codigo}>
                {conta.codigo} - {conta.nome}
              </option>
            ))}
          </select>
        </label>
        <label className="campo">
          <span className="campo-rotulo">Conta crédito</span>
          <select
            value={form.conta_credito}
            onChange={(e) => setForm({ ...form, conta_credito: e.target.value })}
            disabled={emModoEstorno}
            required
          >
            <option value="">Selecione</option>
            {contas.map((conta) => (
              <option key={conta.codigo} value={conta.codigo}>
                {conta.codigo} - {conta.nome}
              </option>
            ))}
          </select>
        </label>
        <label className="campo">
          <span className="campo-rotulo">Valor</span>
          <input
            type="number"
            step="0.01"
            min="0.01"
            value={form.valor}
            onChange={(e) => setForm({ ...form, valor: e.target.value })}
            disabled={emModoEstorno}
            required
          />
        </label>
        <label className="campo">
          <span className="campo-rotulo">Histórico</span>
          <input
            type="text"
            value={form.historico}
            onChange={(e) => setForm({ ...form, historico: e.target.value })}
          />
        </label>
        <button type="submit" className="botao">
          {emModoEstorno ? "Confirmar estorno" : "Lançar"}
        </button>
        {emModoEstorno && (
          <button type="button" className="botao" onClick={aoCancelarEstorno}>
            Cancelar
          </button>
        )}
      </form>
      {erroForm && <p className="erro">{erroForm}</p>}

      <h3>Importar CSV</h3>
      <p>Colunas esperadas: data, conta_debito, conta_credito, valor, historico</p>
      <input type="file" accept=".csv" onChange={aoSelecionarArquivo} />
      {resultadoUpload && (
        <div>
          <p>{resultadoUpload.inseridos} lançamento(s) inserido(s).</p>
          {resultadoUpload.erros.length > 0 && (
            <ul className="erro">
              {resultadoUpload.erros.map((erro) => (
                <li key={erro.linha}>
                  Linha {erro.linha}: {erro.motivo}
                </li>
              ))}
            </ul>
          )}
        </div>
      )}

      <h3>Lançamentos existentes</h3>
      <div className="tabela-rolagem">
        <table>
          <thead>
            <tr>
              <th>Data</th>
              <th>Débito</th>
              <th>Crédito</th>
              <th className="razao-valor">Valor</th>
              <th>Histórico</th>
              <th>Ações</th>
            </tr>
          </thead>
          <tbody>
            {lancamentos.map((lancamento) => (
              <tr key={lancamento.id}>
                <td>{formatarData(lancamento.data)}</td>
                <td>{lancamento.conta_debito}</td>
                <td>{lancamento.conta_credito}</td>
                <td className={classeValor(lancamento.valor)}>
                  {fmt(lancamento.valor)}
                </td>
                <td>
                  {lancamento.historico}
                  {lancamento.estorno_de != null &&
                    ` (estorno do lançamento #${lancamento.estorno_de})`}
                  {estornadoPor[lancamento.id] !== undefined &&
                    ` (estornado pelo lançamento #${estornadoPor[lancamento.id]})`}
                </td>
                <td>
                  <button
                    type="button"
                    className="botao"
                    onClick={() => aoEstornar(lancamento)}
                  >
                    Estornar
                  </button>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </section>
  );
}
```

      Detalhes que **não** são opcionais:

      - O botão da linha é `type="button"`. Sem isso, se um dia a tabela
        acabar dentro de um `<form>`, ele submeteria.
      - Os campos travados usam `disabled`, não `readOnly`: `<select>` não tem
        `readOnly`, e `disabled` é o que comunica "não é editável".
      - `required` continua nos campos travados. `disabled` remove o campo da
        validação nativa do formulário, e isso é correto aqui: o valor deles
        vem do original, não do usuário.
      - Nenhuma classe CSS nova. `<th>Ações</th>` é o único texto de interface
        que o design não nomeia literalmente; qualquer outro rótulo visível é
        exatamente o do design ("Estornar", "Confirmar estorno", "Cancelar",
        "(estorno do lançamento #N)", "(estornado pelo lançamento #M)").
      - O botão "Estornar" aparece em **toda** linha, sem exceção — inclusive
        num estorno e num lançamento já estornado.

- [ ] **Rodar o build** — a partir de `frontend/`:

```
npm run build
```

      Esperado: termina com `✓ built in …`, sem erro. Qualquer erro de
      import/JSX quebra aqui.

- [ ] **Commitar** na branch `feat/estorno`:

```
git add frontend/src/api.js frontend/src/pages/Lancamentos.jsx
git commit -m "Estorno no frontend: botão por linha, modo estorno do formulário e indicações na tabela"
```

---

## Verificação final (coordenador, depois das duas tarefas)

1. `cd backend && python -m pytest -q` → `59 passed`.
2. `cd frontend && npm run build` → sem erro.
3. Apagar `backend/contabilidade.db` à mão (passo manual, ver acima), subir
   backend e frontend, e no navegador: lançar algo, clicar "Estornar" na
   linha, confirmar que os três campos travam com os valores invertidos, que o
   botão diz "Confirmar estorno", que "Cancelar" destrava e limpa, e que
   depois de confirmar as duas linhas mostram "(estorno do lançamento #N)" e
   "(estornado pelo lançamento #M)". Conferir na aba Balancete que os saldos
   das duas contas voltaram a zero (os totais de débito/crédito, não — eles
   dobram, e isso é o esperado).
